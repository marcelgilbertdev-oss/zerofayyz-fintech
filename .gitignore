# Dependencies
node_modules/

# Next.js and build output
.next/
out/
dist/
build/
coverage/
# SvelteKit regenerates this on every `svelte-kit sync`, which every one of the
# client's scripts runs first — so the committed copy was never read, only
# churned. Sixty files of hashed chunks, dirtying the tree after each build.
.svelte-kit/

# Environment variables and secrets
.env
.env.*
!.env.example
# Local Postgres credentials in a name no .env* pattern catches.
env.txt

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Local databases and runtime data
*.sqlite
*.sqlite3
data/
postgres-data/
mongo-data/

# Test artifacts
playwright-report/
test-results/

# Editor and operating-system files
.DS_Store
.idea/
.vscode/

# Temporary files
*.tmp
*.swp

# Internal session notes — never publish
docs/runbooks/HANDOFF.md
.vercel
.env*

# Vercel CLI link state (per-deploy, not shared)
.vercel/

# Go build output
services/reconciler/reconciler
services/reconciler/cmd/reconcile/reconcile
