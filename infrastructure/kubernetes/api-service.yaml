apiVersion: v1
kind: Service
metadata:
  name: zerofayyz-api
  labels:
    app.kubernetes.io/name: zerofayyz-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: zerofayyz-api
  ports:
    - name: http
      port: 80
      targetPort: http
