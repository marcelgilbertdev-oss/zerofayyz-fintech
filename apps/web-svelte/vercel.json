{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": [
    "sin1"
  ],
  "installCommand": "npm --prefix packages/api-contract ci && npm --prefix apps/web-svelte ci",
  "buildCommand": "npm --prefix apps/web-svelte run build",
  "outputDirectory": "apps/web-svelte/build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://zerofayyz-fintech-api.onrender.com/api/:path*"
    }
  ]
}
