// Single source of truth shared with the Vue client so the contract cannot
// drift between frontends. See packages/api-contract/README.md.
export * from "../../../../packages/api-contract/schemas";
